set(EVENTS_LIBRARY_INC 
    ${CMAKE_CURRENT_SOURCE_DIR}/Inc 
)

set(EVENTS_LIBRARY_SRC

)

add_library(EVENTS_LIBRARY INTERFACE)
target_include_directories(EVENTS_LIBRARY INTERFACE ${EVENTS_LIBRARY_INC})
target_sources(EVENTS_LIBRARY INTERFACE ${EVENTS_LIBRARY_SRC})